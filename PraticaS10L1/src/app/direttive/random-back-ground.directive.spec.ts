import { RandomBackGroundDirective } from './random-back-ground.directive';

describe('RandomBackGroundDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomBackGroundDirective();
    expect(directive).toBeTruthy();
  });
});
